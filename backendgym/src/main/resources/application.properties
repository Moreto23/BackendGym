# ============================
# MERCADO PAGO (Sandbox/Prod)
# ============================
mp.access.token=${MP_ACCESS_TOKEN}

mp.back.success=${MP_BACK_SUCCESS}
mp.back.failure=${MP_BACK_FAILURE}
mp.back.pending=${MP_BACK_PENDING}

mp.notification.url=${MP_NOTIFICATION_URL}

# ============================
# BASE DE DATOS
# ============================
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}

# ============================
# JWT
# ============================
app.jwt.secret=${JWT_SECRET:F93A1C7E9B204A51C9F7D3A8E4B6F12A7D9C3F4A1B6D8E2F9C3A7E1B5D9F42C7}
app.jwt.expiration-minutes=60

# ============================
# JPA
# ============================
spring.jpa.hibernate.ddl-auto=update

# ============================
# MAIL
# ============================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USER}
spring.mail.password=${MAIL_PASS}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

spring.mail.default-encoding=UTF-8
spring.mail.test-connection=true
spring.mail.properties.mail.debug=true

# ============================
# SERVIDOR (Railway usa PORT)
# ============================
server.port=${PORT:8080}
